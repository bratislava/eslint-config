{
  "name": "eslint-config-monorepo",
  "version": "0.3.2",
  "private": true,
  "description": "Shareable ESLint configurations for Bratislava projects",
  "license": "EUPL-1.2",
  "repository": {
    "type": "git",
    "url": "https://github.com/bratislava/eslint-config.git"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "publish:all": "npm publish --access public --workspace packages/base && npm publish --access public --workspace packages/nest && npm publish --access public --workspace packages/next && npm publish --access public --workspace packages/react"
  },
  "devDependencies": {
    "@eslint/js": "9.34.0",
    "@eslint/markdown": "6.6.0",
    "eslint": "9.39.2",
    "eslint-config-prettier": "10.1.8",
    "eslint-plugin-simple-import-sort": "12.1.1",
    "globals": "16.5.0"
  },
  "engines": {
    "node": ">=18"
  },
  "volta": {
    "node": "24.12.0"
  }
}
